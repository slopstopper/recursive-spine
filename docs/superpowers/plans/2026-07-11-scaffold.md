# Vertebra 2: Scaffold Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship `recursive-spine-scaffold` — the second vertebra: a sibling skill that stamps a rules codex, ADR directory, CI gate skeleton, and session-memory convention via frames + interview + pollen — with two seed pollen records and the surfacing mechanisms, then run the recursion test against this repo.

**Architecture:** One new skill file plus five neutral frame files under `reference/templates/scaffold/`. Content comes only from frames (structure), the interview (the builder's discipline), and configured hives (proven pollen). Transplant recording delegates to the pollinate skill's pull-mode procedure. Everything else is packaging and the recursion test.

**Tech Stack:** Markdown skill files, GitHub CLI (`gh`), GitHub Actions (bash/grep/jq gates), Claude Code plugin manifest.

## Global Constraints

Copied from the spec (`docs/superpowers/specs/2026-07-11-scaffold-design.md`) and house rules:

- Skill name is self-prefixed: `recursive-spine-scaffold`.
- **Nothing invented ships; structure ships.** Frames contain named empty sections only — no example content, no default rules, no invented demo text (recursion doctrine).
- **Interview-driven, never shipped defaults**: every part optional; declines recorded in the dialect note; "already present" is a valid recorded answer.
- **Offers, never requirements**: plumb-line and tokenomics wiring are offers; no skill text names a dependency.
- **One canonical transplant-recording path**: scaffold delegates to `recursive-spine-pollinate` pull mode; it implements no second recording procedure.
- **Moment-tuned descriptions**: every `description:` frontmatter opens with the moment of use ("Use when …"), not the feature.
- Plugin version becomes exactly `0.5.0`; manifests say **six** skills.
- Dry-run before live stamping (house convention); diffs shown before every write; idempotent re-runs.
- Vocabulary: spine **handover** ≠ tokenomics **handoff**; never merge the words.
- Pollen front-matter must pass the existing CI gate: keys `id, form, source, captured, stage, transplants`; `form` ∈ snippet|pattern|skill-candidate|config; `stage` ∈ seedling|transplanted|graduated.
- README's maturity gate: vertebra 2 flips to "shipped" only in the recursion-test task (Task 7), never earlier.

---

### Task 1: Seed pollen record — `truth-gate-ci` (+ paired issue)

**Files:**
- Create: `pollen/truth-gate-ci.md`

**Interfaces:**
- Produces: pollen id `pollen-truth-gate-ci`, offered by the scaffold's CI part (Task 4) and referenced in the recursion test (Task 7).

- [ ] **Step 1: Write the record**

Create `pollen/truth-gate-ci.md` with exactly:

```markdown
---
id: pollen-truth-gate-ci
form: pattern
source: slopstopper/recursive-spine#3
captured: 2026-07-11
stage: seedling
transplants: []
---

# Truth-gate CI

CI steps that grep/`jq` the repo's own claims and fail loud when a claim
goes stale — treating documentation honesty as a testable invariant, not
a review-time hope.

## What worked

Small bash steps in a plain GitHub Actions workflow, one named step per
invariant. In live use in two repos:

- `slopstopper/recursive-spine` `.github/workflows/validate.yml`:
  manifest parses and names the plugin (`jq -e`), every skill carries
  `name:` + `description:` frontmatter, README never overstates maturity
  (`! grep -inE 'battle-tested|production-ready'`), every pollen record
  carries the schema keys and enum values.
- `slopstopper/tokenomics` `.github/workflows/gates.yml`: bans model
  names (`Fable|Opus|Sonnet|Haiku`) everywhere except the one file
  allowed to name them — lane vocabulary stays model-agnostic by force.

## Why it worked

Stale claims are the cheapest thing to ship and the most expensive to
discover socially ("the README lied"). Each gate costs one grep at CI
time and turns a silent drift into a red X. The gate is also a recorded
decision: reading the workflow tells you exactly which claims the repo
considers load-bearing.

## How to transplant

1. Identify claims that are expensive if silently wrong (counts of
   shipped things, maturity words, banned vocabulary, schema promises).
2. One named workflow step per claim; the step name states the claim
   ("Pollen records carry the required front-matter").
3. Express the check as grep/`jq` against the repo's own files; collect
   failures into a `fail` flag and `exit $fail` — report every miss, not
   just the first.
4. Never gate on things a human must judge; gates hold *mechanical*
   truth (presence, count, enum, absence) and leave judgment to review.
```

- [ ] **Step 2: Run the pollen schema gate locally, expect PASS**

```bash
for k in id form source captured stage transplants; do
  head -20 pollen/truth-gate-ci.md | grep -q "^$k:" || echo "missing $k"
done
head -20 pollen/truth-gate-ci.md | grep -qE '^form: (snippet|pattern|skill-candidate|config)$' || echo "bad form"
head -20 pollen/truth-gate-ci.md | grep -qE '^stage: (seedling|transplanted|graduated)$' || echo "bad stage"
echo done
```

Expected output: `done` only.

- [ ] **Step 3: File the paired issue**

```bash
gh issue create -R slopstopper/recursive-spine --label pollen \
  --title "pollen: truth-gate-ci (pattern, seedling)" \
  --body "Registry record: \`pollen/truth-gate-ci.md\` (lands with the #32 branch).

Captured while building vertebra 2 (scaffold), which offers this pattern in its CI part. Proof: recursive-spine \`validate.yml\` and tokenomics \`gates.yml\`, both in live use — slopstopper scope, public hive.

- form: pattern · stage: seedling · source: slopstopper/recursive-spine#3

Transplant events land here as comments and as \`transplants:\` appends on the record. Closes only on graduation or retirement."
```

- [ ] **Step 4: Commit**

```bash
git add pollen/truth-gate-ci.md
git commit -m "feat(pollen): capture truth-gate-ci pattern (seed pollen for the scaffold, #32)"
```

---

### Task 2: Seed pollen record — `layered-session-memory` (declassified) (+ paired issue)

**Files:**
- Create: `pollen/layered-session-memory.md`

**Interfaces:**
- Produces: pollen id `pollen-layered-session-memory`, offered by the scaffold's session-memory part (Task 4).

- [ ] **Step 1: Write the record**

Create `pollen/layered-session-memory.md` with exactly:

```markdown
---
id: pollen-layered-session-memory
form: pattern
source: slopstopper/recursive-spine#32
captured: 2026-07-11
stage: seedling
transplants: []
---

# Layered session memory

A session-memory convention with time-layered files and explicit rotation
rules, so an agent (or a returning human) reads the smallest file that
answers "where was I?" instead of one ever-growing log.

**Provenance, declassified:** proven in the builder's live session
environment (daily use across their projects, 2026). Recorded here as a
structure-faithful abstraction per the hive routing rule — layout and
rotation only, no personal content. The `source:` anchor is the issue
where capture was approved (#32 design review), because the proving
ground is not a repo.

## What worked

A single memory directory with four layers, hottest to coldest:

| layer | file(s) | holds | rotates |
| --- | --- | --- | --- |
| buffer | `now.md` | the current session's working notes | cleared into today's file at session end |
| daily | `today-<date>.md` | one file per working day | folded into `recent.md` after the day closes |
| recent | `recent.md` | a rolling ~7-day window, compressed | entries older than the window move to archive |
| archive | `archive.md` | everything older, tersest form | never deleted, only compressed |

Plus one file outside the rotation: `core-memories.md` — durable facts
and key moments that must survive every compression.

## Why it worked

Reading cost tracks recency: the buffer is tiny and always current; the
archive is complete but never read by default. Compression at each
boundary is a deliberate act (what mattered today?), which doubles as
review. Nothing is ever lost, but nothing old is ever in the way.

## How to transplant

1. Pick the memory root (a dot-directory at repo or home level).
2. Create the four layers plus the durable file; empty is fine.
3. Adopt the two rotation rules: session end → buffer folds into daily;
   day/window end → older layers compress downward.
4. State the convention in a doc agents read at session start (the
   scaffold's session-memory part stamps exactly this doc).
```

- [ ] **Step 2: Run the pollen schema gate locally, expect PASS**

```bash
for k in id form source captured stage transplants; do
  head -20 pollen/layered-session-memory.md | grep -q "^$k:" || echo "missing $k"
done
head -20 pollen/layered-session-memory.md | grep -qE '^form: (snippet|pattern|skill-candidate|config)$' || echo "bad form"
head -20 pollen/layered-session-memory.md | grep -qE '^stage: (seedling|transplanted|graduated)$' || echo "bad stage"
echo done
```

Expected output: `done` only.

- [ ] **Step 3: File the paired issue**

```bash
gh issue create -R slopstopper/recursive-spine --label pollen \
  --title "pollen: layered-session-memory (pattern, seedling, declassified)" \
  --body "Registry record: \`pollen/layered-session-memory.md\` (lands with the #32 branch).

Declassified capture per the hive routing rule in \`docs/tracking-dialect.md\`: proof is the builder's live session environment, so the record carries a structure-faithful abstraction (layers + rotation) with scrubbed provenance and no personal content. Owner approved capture at #32 design review.

- form: pattern · stage: seedling · source: slopstopper/recursive-spine#32

Transplant events land here as comments and as \`transplants:\` appends on the record. Closes only on graduation or retirement."
```

- [ ] **Step 4: Commit**

```bash
git add pollen/layered-session-memory.md
git commit -m "feat(pollen): capture layered-session-memory pattern, declassified (seed pollen for the scaffold, #32)"
```

---

### Task 3: The five frame files

**Files:**
- Create: `reference/templates/scaffold/codex-frame.md`
- Create: `reference/templates/scaffold/adr-frame.md`
- Create: `reference/templates/scaffold/adr-readme.md`
- Create: `reference/templates/scaffold/ci-frame.yml`
- Create: `reference/templates/scaffold/memory-convention-frame.md`

**Interfaces:**
- Produces: the exact file paths above; Task 4's skill text references them via `${CLAUDE_PLUGIN_ROOT}/reference/templates/scaffold/<name>`.

Frames are structure only. Every fillable slot is an HTML comment naming which interview answer fills it. No example content anywhere.

- [ ] **Step 1: Write `codex-frame.md`**

```markdown
# <REPO_NAME> — rules codex

<!-- Each section below is filled from the scaffold interview. Sections
     the builder declines are OMITTED from the stamped file, never left
     empty or filled with boilerplate. -->

## Mission

<!-- interview: one or two sentences, the builder's words -->

## House rules

<!-- interview: the builder's actual working rules, one bullet each.
     No rule was offered as a default; every line here was said by the
     builder. -->

## Tracking (recursive-spine convention)

<!-- bootstrap's tracking-section.md slots in here unchanged, with its
     substitutions already applied. The scaffold never rewrites it. -->

## Session memory

<!-- filled from the session-memory part when accepted: one line naming
     where the convention doc lives. Omitted when that part is declined. -->

## CI gates

<!-- filled from the CI part when accepted: one line naming the workflow
     file and what it holds true. Omitted when that part is declined. -->

## Moments map

<!-- interview, seeded with the spine's own moments; the builder edits,
     adds their own, removes what they don't use. One line per moment.
     Format: "<moment> → <skill or action>". Seeds offered:
     - postponing something → file it with the deferral label (recursive-spine-method)
     - something just proved itself → capture it (recursive-spine-pollinate)
     - closing a unit of work → file debts + ask the pollen question (principle 4)
     - "where does work stand?" → recursive-spine-digest
     - stamping a new repo → recursive-spine-bootstrap, then recursive-spine-scaffold -->
```

- [ ] **Step 2: Write `adr-frame.md`**

```markdown
# ADR-<NNNN>: <TITLE>

**Status:** <!-- proposed | accepted | superseded by ADR-<NNNN> -->
**Date:** <YYYY-MM-DD>

## Context

<!-- what forced a decision — the real situation, not a rationale
     written after the fact -->

## Decision

<!-- what was decided, stated as a decision ("We will …") -->

## Consequences

<!-- what this makes easier, what it makes harder, what it forecloses.
     Honest costs belong here. -->
```

- [ ] **Step 3: Write `adr-readme.md`**

```markdown
# Architecture decision records

One file per decision, numbered `NNNN-<slug>.md`, append-only.

- Numbering is sequential and never reused.
- A recorded ADR is immutable: to change course, write a new ADR and mark
  the old one `superseded by ADR-<NNNN>`. Never edit a decision into
  something it wasn't.
- An ADR records a *real* decision that was actually taken — backfilled
  examples must be real history, never invented demos.
```

- [ ] **Step 4: Write `ci-frame.yml`**

```yaml
name: gates
on:
  push: { branches: [main] }
  pull_request:
permissions:
  contents: read
jobs:
  gates:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      # One named step per gate, from the scaffold interview. A gate holds
      # a MECHANICAL invariant (presence, count, enum, absence) that would
      # be expensive to break silently. The step name states the claim.
      # Pattern:
      # - name: <claim that must stay true>
      #   run: |
      #     fail=0
      #     <grep/jq check> || { echo "<what broke>"; fail=1; }
      #     exit $fail
```

- [ ] **Step 5: Write `memory-convention-frame.md`**

```markdown
# Session memory — <REPO_NAME>

<!-- filled from the scaffold interview; the layered-session-memory
     pollen record is offered as proven filling where a hive holds it -->

## Where memory lives

<!-- interview: the memory root path -->

## Layers

<!-- interview: each layer, what it holds, hottest to coldest -->

## Rotation

<!-- interview: when each layer folds into the next; who does it -->

## What never enters memory

<!-- interview: secrets, personal data, anything the builder excludes -->
```

- [ ] **Step 6: Commit**

```bash
git add reference/templates/scaffold/
git commit -m "feat(scaffold): five neutral frame files — structure ships, content never does (#32)"
```

---

### Task 4: The skill — `skills/recursive-spine-scaffold/SKILL.md`

**Files:**
- Create: `skills/recursive-spine-scaffold/SKILL.md`

**Interfaces:**
- Consumes: frame paths from Task 3; pollen ids `pollen-truth-gate-ci`, `pollen-layered-session-memory` (Tasks 1–2) as the worked offers.
- Produces: skill name `recursive-spine-scaffold` (Task 6 manifests, Task 7 recursion test).

- [ ] **Step 1: Write the skill file**

Create `skills/recursive-spine-scaffold/SKILL.md` with exactly:

```markdown
---
name: recursive-spine-scaffold
description: Use when a repo has (or is getting) the tracking stamp and needs the rest of its spine — interviews for and stamps up to four parts, each optional: a rules codex with a moments map, an ADR directory, a CI gate skeleton, and a session-memory convention. Frames + the builder's answers + proven pollen from their hives; nothing invented ships. Offers recursive-spine-bootstrap first when tracking is missing; records every answer, including declines, in the dialect note.
---

# recursive-spine: scaffold

Stamp the rest of the spine onto the current repo. Read
`${CLAUDE_PLUGIN_ROOT}/reference/principles.md` first. Content comes from
three sources only: the frames in
`${CLAUDE_PLUGIN_ROOT}/reference/templates/scaffold/` (structure), the
interview (the builder's discipline), and the builder's configured hives
(proven pollen). If a part's content would have to be invented, the part
is not stamped.

## 1. Preflight

- `gh auth status` — must be authenticated; `gh repo view --json
  nameWithOwner` — must be a GitHub repo.
- Tracking check: does the repo have a dialect note
  (`docs/tracking-dialect.md` or the repo's equivalent)? If not, **offer
  recursive-spine-bootstrap first** — tracking is the vertebra this one
  bolts onto. Builder may decline and scaffold anyway; record the decline.
- Hive check: read the dialect note's `pollinate:` section. If no hives
  are configured, offer the recursive-spine-pollinate hive interview; if
  declined, proceed pollen-less and say so loudly in the report — never
  substitute a default hive.

## 2. The four parts (interview one at a time, all optional)

Each part runs the same cycle:
**offer → interview → pollen check → stamp (diff first) → record.**

Offer each part with its one-line failure-mode pitch, then stop for the
answer before moving on:

1. **Rules codex** — "without one, every session re-derives the house
   rules, wrongly." Frame: `codex-frame.md`. Interview fills each
   section in the builder's own words; declined sections are omitted.
   The moments map is seeded with the spine's moments (listed in the
   frame) and edited to the builder's reality. If a CLAUDE.md/AGENTS.md
   already exists, show a merged proposal — never overwrite silently.
   Ask which filename the repo uses before writing.
2. **ADR directory** — "undocumented decisions get re-litigated by
   people who weren't there." Frames: `adr-frame.md`, `adr-readme.md`.
   Interview: where do docs live (offer the repo's existing convention),
   and is there a *real* recent decision to backfill as ADR-0001? An
   invented example ADR is banned; no real decision → the directory
   ships with only its README.
3. **CI gate skeleton** — "claims that nothing checks go stale
   silently." Frame: `ci-frame.yml`. Interview applies the routing test:
   *"which invariants, if silently broken, would be expensive?"* — each
   answer becomes one named gate step. Pollen offer: `truth-gate-ci`.
   If a CI workflow already exists, offer to extend it instead; "already
   present" is a valid recorded answer.
4. **Session-memory convention** — "sessions that start from zero pay
   the re-orientation tax every time." Frame:
   `memory-convention-frame.md`. Pollen offer: `layered-session-memory`.
   This stamps a convention *doc*, never tooling — harness-specific
   tooling is out of scope by design.

## 3. Pollen consumption

For each accepted part, search the configured hives' registries for
records whose `form` and content match the part (raw-fetch `pollen/` as
primary, clone as fallback — the pollinate skill's own order). Offer
matches with their provenance line. **An acceptance is a transplant**:
record it through recursive-spine-pollinate's pull-mode procedure —
comment on the pollen issue, append the target repo to the record's
`transplants:` list. This skill implements no recording path of its own.
Nothing relevant in the hives → say so and fill from the interview alone.

## 4. Kin offers (never requirements)

- plumb-line installed (a `plumb-line-bootstrap` skill exists)? Offer its
  guard wiring for the stamped gates.
- A tokenomics playbook in the repo? Offer to point the codex's CI/lane
  language at it for semantics.
Both are offers; both answers land in the dialect note.

## 5. Stamp and record

- Dry-run first: run the whole interview, show every generated file, write
  nothing until the builder approves the set.
- Idempotent: re-running proposes diffs against what exists; it never
  duplicates sections, ADR numbers, or workflow steps.
- Record every part's answer — accepted (with what filled it), declined
  (with the builder's reason, one line), or already-present — in the
  dialect note under `## scaffold`.

## 6. Report

End with: parts stamped (files written), parts declined or already
present (and why), pollen offered vs. accepted (transplants recorded),
kin offers and answers, and the repo's next natural moment: "when
something here proves itself, recursive-spine-pollinate captures it."
Honest denominator throughout — skipped means listed.

## Never

- Never invent content for a frame slot the interview left empty.
- Never overwrite an existing codex, workflow, or ADR silently.
- Never stamp a part the builder declined, or record a decline as
  anything but a decline.
- Never bypass the pollinate skill's recording procedure for transplants.
- Never require plumb-line or tokenomics for any function.
```

- [ ] **Step 2: Run the frontmatter gate locally, expect PASS**

```bash
head -20 skills/recursive-spine-scaffold/SKILL.md | grep -q '^name:' && \
head -20 skills/recursive-spine-scaffold/SKILL.md | grep -q '^description:' && \
echo PASS
```

Expected output: `PASS`.

- [ ] **Step 3: Commit**

```bash
git add skills/recursive-spine-scaffold/SKILL.md
git commit -m "feat(scaffold): the recursive-spine-scaffold skill — frames + interview + pollen (#32)"
```

---

### Task 5: Surfacing — description audit + referral seams

**Files:**
- Modify: `skills/recursive-spine-bootstrap/SKILL.md` (report section, line ~75-80)
- Modify: `skills/recursive-spine-method/SKILL.md` (module walk area — locate the modules list)

**Interfaces:**
- Consumes: skill name `recursive-spine-scaffold` (Task 4).

- [ ] **Step 1: Audit the five existing descriptions against the moment standard**

The standard: the description opens with the moment of use. Verify each
(current texts, for reference — all five already open "Use when" + a
moment; the audit confirms and records, it does not rewrite for style):

| skill | opens with moment? |
| --- | --- |
| bootstrap | yes — "Use when installing …" |
| digest | yes — "Use when sweeping …" |
| method | yes — "Use when a builder wants to learn …" |
| migrate | yes — "Use when converting …" |
| pollinate | yes — "Use when something just proved itself …" |

Expected outcome: no description changes. If any description had drifted
from this table's text, flag it in the task report rather than silently
rewriting.

- [ ] **Step 2: Add bootstrap → scaffold referral seam**

In `skills/recursive-spine-bootstrap/SKILL.md`, section `## 4. Report`,
append to the end of the section's paragraph:

```markdown
If the repo now wants the rest of its spine — rules codex, ADRs, CI
gates, session memory — offer `recursive-spine-scaffold` as the natural
next step (offer, not upsell: one line, their choice).
```

- [ ] **Step 3: Add scaffold to the method skill's module walk**

In `skills/recursive-spine-method/SKILL.md`, find the list or paragraph
that walks the plugin's skills/modules (it gained a pollination bullet in
PR #43). Add one bullet in the same voice, after the pollination one:

```markdown
- **Scaffold** (`recursive-spine-scaffold`): stamps the non-tracking
  vertebra parts — rules codex with a moments map, ADR directory, CI
  gate skeleton, session-memory convention — frames + the builder's
  interview + proven pollen; every part optional, declines recorded.
```

- [ ] **Step 4: Run the frontmatter gate over all skills, expect PASS**

```bash
fail=0
for f in skills/*/SKILL.md; do
  head -20 "$f" | grep -q '^name:' || { echo "missing name: $f"; fail=1; }
  head -20 "$f" | grep -q '^description:' || { echo "missing description: $f"; fail=1; }
done
echo "fail=$fail"
```

Expected output: `fail=0`.

- [ ] **Step 5: Commit**

```bash
git add skills/recursive-spine-bootstrap/SKILL.md skills/recursive-spine-method/SKILL.md
git commit -m "feat(surfacing): referral seams bootstrap→scaffold and method module walk; description audit clean (#32)"
```

---

### Task 6: Packaging — manifests and README count

**Files:**
- Modify: `.claude-plugin/plugin.json`
- Modify: `.claude-plugin/marketplace.json`
- Modify: `README.md` (skill enumeration only — NOT the vertebra status, which is Task 7's gate)

**Interfaces:**
- Consumes: skill name `recursive-spine-scaffold` (Task 4).

- [ ] **Step 1: Update `plugin.json`**

Set `version` to `0.5.0`; replace `description` with:

```
GitHub issues+milestones tracking convention and project scaffold: recursive-spine-method, recursive-spine-bootstrap, recursive-spine-migrate, recursive-spine-digest, recursive-spine-pollinate, recursive-spine-scaffold. State lives where it's queryable, not where it merges — what proves itself travels, and the spine stamps the rest.
```

Append keywords `"scaffold"` and `"adr"` to the existing keywords array.

- [ ] **Step 2: Update `marketplace.json`**

Replace the plugin entry's `description` with:

```
GitHub issues+milestones tracking convention and project scaffold — six skills (recursive-spine-method, recursive-spine-bootstrap, recursive-spine-migrate, recursive-spine-digest, recursive-spine-pollinate, recursive-spine-scaffold). State lives where it's queryable, not where it merges. Get oriented: run recursive-spine-method first.
```

- [ ] **Step 3: Update README's skill enumeration**

In `README.md`: in the opening section, after the sentence ending
"…pulls them into others.", append:

```markdown
The second vertebra is the scaffold: `recursive-spine-scaffold` stamps
the rest — rules codex, ADR directory, CI gate skeleton, session-memory
convention — from frames, the builder's interview, and proven pollen.
```

In the Install section, change "the second installs the five skills" to
"the second installs the six skills".

Do **not** touch the vertebra-status list in this task.

- [ ] **Step 4: Run the manifest gate locally, expect PASS**

```bash
jq -e '.name == "recursive-spine" and (.description | length > 0) and .version == "0.5.0"' .claude-plugin/plugin.json && \
jq -e . .claude-plugin/marketplace.json > /dev/null && echo PASS
```

Expected output: `true` then `PASS`.

- [ ] **Step 5: Commit**

```bash
git add .claude-plugin/plugin.json .claude-plugin/marketplace.json README.md
git commit -m "chore(plugin): v0.5.0 — six skills, scaffold packaged (#32)"
```

---

### Task 7: Recursion test — scaffold this repo, honestly

**Files:**
- Create: `docs/adr/README.md` (from `adr-readme.md` frame)
- Create: `docs/adr/0001-scaffold-as-sibling-skill.md`
- Modify: `docs/tracking-dialect.md` (new `## scaffold` section at end)
- Modify: `README.md` (vertebra 2 status flip)

**Interfaces:**
- Consumes: the skill's part cycle (Task 4), ADR frames (Task 3).

This task *executes* the scaffold against this repo with the owner's
honest answers, pre-decided in the approved spec except where noted:

| part | answer | why |
| --- | --- | --- |
| rules codex | **owner checkpoint** — ask one question at execution | spec leaves it to the owner |
| ADR directory | accept; backfill ADR-0001 | real decision exists (sibling-skill choice) |
| CI gates | already present (`validate.yml`) | truth-gate source repo |
| session memory | decline — repo-level memory not in use here | honest answer |

No transplants are recorded: this repo is the source of both seed pollen;
"already present" is the truthful pull-mode answer (spec: first real
transplants remain #42).

- [ ] **Step 1: Owner checkpoint — the codex question**

Ask the owner exactly one question: "Scaffolding this repo: want a rules
codex (CLAUDE.md/AGENTS.md with mission, house rules, moments map)? If
yes, which rules go in it — say them in your words." If the session is
running autonomously with no owner response available, record the codex
part as `pending owner interview` in the dialect note (a loud, honest
state) and continue; do not invent rules.

- [ ] **Step 2: Stamp the ADR directory**

Write `docs/adr/README.md` with the exact `adr-readme.md` frame content
from Task 3, Step 3.

Write `docs/adr/0001-scaffold-as-sibling-skill.md`:

```markdown
# ADR-0001: Scaffold is a sibling skill, not grown bootstrap

**Status:** accepted
**Date:** 2026-07-11

## Context

Issue #32 left the shape open: grow `recursive-spine-bootstrap` to stamp
the full spine, or add a sibling skill. Bootstrap is small and proven
(the tracking stamp); the scaffold is new and larger (four parts,
interview-heavy). One skill would mean one entry point but a long
interview and a proven/unproven mix in one file.

## Decision

We will ship the scaffold as a sibling skill, `recursive-spine-scaffold`.
Its preflight checks for the tracking stamp and offers bootstrap first —
the dependency points at the proven thing. Bootstrap is unchanged except
a one-line referral seam in its report.

## Consequences

Easier: each skill keeps one concern; tracking-only users never meet the
scaffold interview; the scaffold can evolve without risking the proven
stamp. Harder: two invocations for a fresh repo (mitigated by the
referral seams); the plugin's skill count grows, which raises the
surfacing burden the moments map exists to carry.
```

- [ ] **Step 3: Record the run in the dialect note**

Append to `docs/tracking-dialect.md`:

```markdown
## scaffold (this installation)

Recorded per the scaffold skill's interview, run against this repo
(recursion: the module's first target is the repo that ships it).

- **Rules codex:** <owner's answer from Step 1 — accepted with their
  rules, declined, or "pending owner interview">
- **ADR directory:** accepted — `docs/adr/`, ADR-0001 backfilled from
  the real sibling-skill decision (#32).
- **CI gates:** already present — `.github/workflows/validate.yml` is
  this repo's gate set and the source proof of the `truth-gate-ci`
  pollen. Nothing stamped.
- **Session memory:** declined — repo-level session memory is not in use
  here; the builder's memory convention lives at the environment level
  (see the `layered-session-memory` pollen record).
- **Pollen pulls:** none recorded — this repo is the source of both seed
  records; "already present" is the truthful answer. First real
  transplants remain #42.
- **Kin offers:** plumb-line guard wiring and tokenomics playbook
  pointer — both already answered in "Kin offers, as answered" above;
  not re-asked.
```

(Replace the codex line with the actual answer from Step 1.)

- [ ] **Step 4: Flip vertebra 2 in README**

In `README.md`, replace:

```markdown
- **Vertebra 2 — scaffold: planned**, tracked as #32 — bootstrap stamps a
  full project spine, not just tracking.
```

with:

```markdown
- **Vertebra 2 — scaffold: shipped.** `recursive-spine-scaffold` stamps
  rules codex, ADRs, CI gates, and session memory from frames + the
  builder's interview + proven pollen. Recursion test run against this
  repo (see `docs/tracking-dialect.md`, "scaffold").
```

- [ ] **Step 5: Run all local gates, expect all PASS**

```bash
jq -e '.version == "0.5.0"' .claude-plugin/plugin.json > /dev/null && echo manifests-ok
fail=0
for f in skills/*/SKILL.md; do
  head -20 "$f" | grep -q '^name:' && head -20 "$f" | grep -q '^description:' || fail=1
done
for f in pollen/*.md; do
  [ "$(basename "$f")" = "README.md" ] && continue
  for k in id form source captured stage transplants; do head -20 "$f" | grep -q "^$k:" || fail=1; done
done
! grep -inE 'battle-tested|production-ready' README.md || fail=1
echo "fail=$fail"
```

Expected output: `manifests-ok` then `fail=0`.

- [ ] **Step 6: Commit**

```bash
git add docs/adr/ docs/tracking-dialect.md README.md
git commit -m "feat(scaffold): recursion test — this repo scaffolded with honest answers; vertebra 2 shipped (#32)"
```

---

## Final: PR

Open a draft PR from `feat/32-scaffold` titled
"feat: vertebra 2 — the scaffold (#32)" with `Closes #32` in the body,
summarizing: the skill, five frames, two seed pollen (+ the in-flow
`moment-based-skill-surfacing` record already on the branch, issue #49),
surfacing seams, v0.5.0, and the recursion-test answers table (including
any `pending owner interview` state, stated plainly).
